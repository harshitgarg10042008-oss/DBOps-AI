import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { toast } from "sonner";

export default function SettingsSurface({ title, icon }: { title: string; icon: React.ReactNode }) {
  const [maxRows, setMaxRows] = useState("100");
  const [timeoutMs, setTimeoutMs] = useState("8000");
  const [sslRequired, setSslRequired] = useState(true);
  return <div className="space-y-6"><div><div className="mb-2 flex items-center gap-2 text-xs uppercase tracking-[.22em] text-pink-300"><span className="h-px w-8 bg-pink-300" /> control settings</div><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center border border-cyan-300/30 bg-cyan-300/5 text-cyan-200">{icon}</div><h2 className="font-display text-3xl font-bold text-white">{title}</h2></div><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Configure the safety contract. These values are local operator preferences until persisted through the secured settings API.</p></div><div className="grid gap-5 xl:grid-cols-2"><Card className="border-white/10 bg-[#0c101a]/85"><CardHeader><CardTitle className="font-display text-sm uppercase tracking-[.14em] text-white">Read-only execution policy</CardTitle></CardHeader><CardContent className="space-y-5"><div><label className="mb-2 block text-[10px] uppercase tracking-widest text-slate-500">Maximum returned rows</label><Input value={maxRows} onChange={event => setMaxRows(event.target.value)} inputMode="numeric" className="border-white/10 bg-white/[.03] text-white" /></div><div><label className="mb-2 block text-[10px] uppercase tracking-widest text-slate-500">Statement timeout (ms)</label><Input value={timeoutMs} onChange={event => setTimeoutMs(event.target.value)} inputMode="numeric" className="border-white/10 bg-white/[.03] text-white" /></div><Button onClick={() => toast.success("Policy draft saved locally; persistence will be enabled with workspace settings storage")} className="rounded-none bg-cyan-300 font-bold text-[#05070c] hover:bg-cyan-200">Save policy draft</Button></CardContent></Card><Card className="border-white/10 bg-[#0c101a]/85"><CardHeader><CardTitle className="font-display text-sm uppercase tracking-[.14em] text-white">Connection and model status</CardTitle></CardHeader><CardContent className="space-y-4"><div className="flex items-center justify-between border border-white/8 p-4"><div><div className="text-sm font-semibold text-white">SSL requirement</div><div className="mt-1 text-xs text-slate-500">Prefer encrypted connections for registered endpoints.</div></div><button onClick={() => setSslRequired(value => !value)} className={`h-6 w-11 border ${sslRequired ? "border-cyan-300 bg-cyan-300/20" : "border-white/20 bg-white/5"}`}><span className={`block h-4 w-4 bg-cyan-200 transition-transform ${sslRequired ? "translate-x-5" : "translate-x-0"}`} /></button></div><div className="flex items-center justify-between border border-white/8 p-4"><div><div className="text-sm font-semibold text-white">LLM provider</div><div className="mt-1 text-xs text-slate-500">Credential handoff pending; no external key is required for this build.</div></div><Badge className="border-pink-300/20 bg-pink-300/5 text-pink-200">placeholder</Badge></div><div className="flex items-center justify-between border border-white/8 p-4"><div><div className="text-sm font-semibold text-white">Workspace profile</div><div className="mt-1 text-xs text-slate-500">DBOps AI / isolated operator workspace</div></div><Badge className="border-cyan-300/20 bg-cyan-300/10 text-cyan-200">active</Badge></div></CardContent></Card></div></div>;
}
